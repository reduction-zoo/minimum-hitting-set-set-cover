# Independent verification, 2026-09-23

Candidate: [algorithm.py](algorithm.py), implementing the rule in [proof.md](proof.md). Prepared corpus check: `uv run python campaigns/minimum-hitting-set-set-cover/work/check.py --candidate campaigns/minimum-hitting-set-set-cover/work/algorithm.py` passed 120 distinct source inputs and 194 separately extracted optimal target outputs. The checker uses locked Z3 4.16.0.0 for target optima and compares every recovered answer to the independently stored source optimum.

Additional check: `uv run python campaigns/minimum-hitting-set-set-cover/work/verify.py --candidate campaigns/minimum-hitting-set-set-cover/work/algorithm.py` passed 689 source instances and 821 target outputs. `verify.py` imports neither candidate nor prepared checker. It enumerates every indexed family of length 0–3 over each universe of size 0–3, independently enumerates all source and target subsets, executes F and G in fresh subprocesses, and requires each recovered output to be one of the globally optimal source outputs. Coverage includes 244 infeasible sources and 123 instances with multiple target optima. There were no mismatches or inconclusive solver results.

After the [initial review](../reviews/initial/review.md), the new `uv run python campaigns/minimum-hitting-set-set-cover/rounds/002/large_sparse.py` failed on the old candidate because a 100,000-element sparse universe produced a target longer than the specified bound. Following the active-support repair, it passed four source instances and four target outputs. These include a 5,001-digit universe size with one selected element near its upper end, an empty family, and an empty member. The prepared and exhaustive suites were rerun on the repaired candidate with their same passing counts. Finite checks cover repeated and empty members, tied optima and extreme sparse encodings; [proof.md](proof.md) supplies the general argument. No large-instance target-solver runtime claim was measured.
